import Vue from 'vue'
import Vuetify from 'vuetify'

Vue.use(Vuetify, {
  theme: {
    primary: '#2AD19A', // a color that is not in the material colors palette
    accent: '#EF1A1C',
    secondary: '#0A1011',
    error: '#EF1A1C',
  }
})
